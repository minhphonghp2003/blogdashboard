import React from 'react'

function ForgotLayout({children}) {
  return (
    <main className='flex justify-center items-center w-full h-[100vh]' id='forgot-layout'>
        {children}
    </main>
  )
}

export default ForgotLayout